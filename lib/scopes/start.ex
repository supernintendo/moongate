defmodule Moongate.Scopes.Start do
  @doc """
    This is called when the game server first loads. This is
    overriden by the Moongate.Scopes.Start within the world's
    modules/scopes directory.
  """
  def on_load do
  end
end
